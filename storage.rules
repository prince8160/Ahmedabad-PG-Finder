rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /pg_images/{imageId} {
      // Allow any user to read (view) images
      allow read: if true;
      // Allow only authenticated users to upload images
      allow write: if request.auth != null;
    }
  }
}
